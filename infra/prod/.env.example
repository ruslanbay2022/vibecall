# Copy to infra/prod/.env on VDS. Do NOT commit real values.
# Generate: LIVEKIT_API_KEY=API$(openssl rand -hex 8)
#           LIVEKIT_API_SECRET=$(openssl rand -base64 32)
LIVEKIT_API_KEY=APIxxxxxxxxxxxx
LIVEKIT_API_SECRET=replace-with-openssl-rand-base64-32
