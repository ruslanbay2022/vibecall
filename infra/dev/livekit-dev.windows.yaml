port: 7880
bind_addresses: ["0.0.0.0"]
rtc:
  tcp_port: 7881
  port_range_start: 50000
  # Narrow range for Docker Desktop on Windows (full 50000-50200 often fails to bind).
  port_range_end: 50015
  use_external_ip: false
  node_ip: 127.0.0.1
keys:
  devkey: devsecretdevsecretdevsecretdevse
logging:
  level: info
