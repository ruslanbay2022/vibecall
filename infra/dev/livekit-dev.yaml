port: 7880
bind_addresses: ["0.0.0.0"]
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50200
  # Force host ICE candidates to 127.0.0.1 (published Docker ports on the host).
  # use_external_ip: true discovers public IP — breaks localhost-only dev.
  use_external_ip: false
  node_ip: 127.0.0.1
keys:
  devkey: devsecretdevsecretdevsecretdevse
logging:
  level: info
